# DatabricksJavaSparkBudgetGenerator
This code does a query in Databricks from a table that has structured budget entries. These entries have group IDs and dates. This code generates a file with entries for each group from the main budget table and stores them in a Databricks volume.
